
module.exports = class SessionHandler {

    static async handle(request, response, next) {

    }

    /**
     * Takes in an express session and sets the user property on the express request resource
     * @param {Express.session} session the express-session session object
     */
    static async fetchUserAndSet(session) {

    }

}